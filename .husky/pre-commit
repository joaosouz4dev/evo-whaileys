#!/usr/bin/env sh
# Desabilitado temporariamente - reative removendo 'exit 0' e descomentando abaixo
exit 0
# [ -f "$HOME/.bashrc" ] && . "$HOME/.bashrc" 2>/dev/null
# [ -f "$HOME/.bash_profile" ] && . "$HOME/.bash_profile" 2>/dev/null
# export PATH="$PATH:/c/Program Files/nodejs:${HOME}/scoop/apps/nodejs/current/bin:${APPDATA}/npm"
# npx lint-staged
